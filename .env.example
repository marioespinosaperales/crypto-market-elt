# Copy to .env and fill in. NEVER commit .env.

# Optional: CoinGecko demo API key (raises the rate limit). https://www.coingecko.com/en/api
ELT_COINGECKO_API_KEY=

# Optional: override DuckDB warehouse path (default: warehouse/crypto.duckdb)
# ELT_DUCKDB_PATH=
