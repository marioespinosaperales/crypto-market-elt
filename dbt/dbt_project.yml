name: crypto_market_elt
version: "1.0.0"
profile: crypto_market_elt

model-paths: ["models"]
test-paths: ["tests"]

models:
  crypto_market_elt:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
