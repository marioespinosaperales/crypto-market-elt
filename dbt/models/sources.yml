version: 2

sources:
  - name: raw
    description: Raw layer loaded from Parquet by the ingestion pipeline.
    schema: raw
    tables:
      - name: coingecko_markets
        description: Daily top-N market-cap snapshot (one row per coin per day).
      - name: binance_klines
        description: Closed daily OHLCV candles per symbol.
