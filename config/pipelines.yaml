# Parámetros del pipeline (rutas, warehouse). Overridable por env vars con prefijo ELT_.
data_dir: ./data                        # raw layer local; en prod apunta a un bucket montado
duckdb_path: ./warehouse/crypto.duckdb  # warehouse embebido
raw_schema: raw                         # schema de DuckDB donde aterrizan los datos crudos
