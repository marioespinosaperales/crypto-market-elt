# Pipeline parameters (paths, warehouse). Overridable via ELT_ env vars.
data_dir: ./data                        # local raw layer; in prod point at a mounted bucket
duckdb_path: ./warehouse/crypto.duckdb  # embedded warehouse
raw_schema: raw                         # DuckDB schema where raw data lands
